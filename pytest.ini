[pytest]
addopts = -m "not demo"
markers =
    smoke: Quick integration tests designed to verify execution health in under a second.
    demo: Slow end-to-end smoke tests that run every demo script as a subprocess (excluded by default; run with -m demo).
